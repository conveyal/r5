/**
 * This package should contain only the HttpController interface and implementations of that interface.
 * These are the classes that define the endpoints of our HTTP API and are registered with the Spark framework.
 */
package com.conveyal.analysis.controllers;